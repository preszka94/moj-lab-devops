# Repo audit — 2026-05-18

Pre-Phase-1 review of `~/DevOps-Lab` and `~/DevOps-Lab/trader-workstation-platform/`
to give us a clean start. Captures: parent-repo inventory, per-file verdicts
in the project, what was changed in this pass, what's still outstanding for
manual approval.

---

## 1. DevOps-Lab inventory (parent repo)

The parent `~/DevOps-Lab` repo is a mix of an older SafeDoc invoice-processing
project and the new `trader-workstation-platform`. The two don't share code,
but they share the same git repo.

| Path | What it is | Status / overlap |
|---|---|---|
| `.git/` | Parent repo Git data. | Active. |
| `.gitignore` | Repo-wide ignore: `venv/`, `docker/minio_data/`, `.DS_Store`, `test_plik.txt`, `__pycache__/`. | Active. Does **not** cover the two committed secret files (see security section). |
| `.gitmodules` | Submodule entry for `openclaw` with a `path` but **no `url`** — malformed. | Broken; needs your call. |
| `README.md` | DevOps-Lab parent README — describes SafeDoc only. | Pre-trader-workstation; outdated. |
| `Trading_App_Note.pdf` | The authoritative Notion roadmap exported as PDF. | Active reference. |
| `GNS3_files/` | A single `.gns3` networking-lab file. | Abandoned (one-off lab). |
| `ansible/inspect-mac.yml`, `inventory.ini` | Generic Ansible playbook that gathers facts about the Mac. | Reusable — Phase 5.1 explicitly reviews this file. |
| `commands.txt` | Loose Oracle/SafeDoc cheatsheet (`docker exec -it oracle-free sqlplus …`, `./process_all.sh`). | SafeDoc artifact; possibly useful Oracle reference for Phase 2. |
| `diagram_business_Piotr_Reszka.bpmn`, `.png` | Business-process diagram. | Unclear origin, likely SafeDoc. |
| `docker/minio_data/` | MinIO runtime state (~1.9 MB). | SafeDoc runtime data; ignored by Git. |
| `dummy_invoice.pdf`, `input_invoices/`, `insert_test_data.py`, `process_all.sh` | SafeDoc test fixtures + driver scripts at the repo root. | SafeDoc; should live under `projects/safedoc/` but sit loose at the root. |
| `github-recovery-codes.txt` | **Real GitHub 2FA recovery codes**, committed to Git. | **CRITICAL SECURITY** — see section 5. |
| `marp_presentations/` | Empty (0 B). | Unused. |
| `projects/safedoc/` | The SafeDoc invoice-processing project proper (Flask, Oracle, MinIO, Ansible, GCP Build). | Active separate project. |
| `scripts/backend_app.py`, `setup_db.py`, `view_invoices.sql` | SafeDoc backend, schema setup, and SQL view. | SafeDoc. |
| `scripts/gcp-credentials.json` | **Real GCP service-account private key** for project `safedoc-flow`, committed to Git. | **CRITICAL SECURITY** — see section 5. |
| `scripts/__pycache__/` | Python bytecode. | Should be ignored — `__pycache__/` is in `.gitignore` but check git ls-files. |
| `test_plik.txt` | Test/scratch file. | Ignored by `.gitignore`. |
| `trader-workstation-platform/` | **This project.** | Active — see section 2. |
| `venv/` (137 MB) | Python virtualenv at the repo root. | Ignored by `.gitignore` for new content; was likely committed before the ignore rule. |
| `web_app/app.py`, `templates/` | Standalone Flask app. | Likely an early SafeDoc experiment; status unclear. |
| `openclaw/` (6.1 GB) | Massive nested TypeScript project with its own `.git`. | Unrelated to either project; included via the broken submodule entry. |

**Top-level verdicts:**
- The DevOps-Lab repo is doing double duty (SafeDoc + trader-workstation-platform).
  Out of scope for this audit, but worth a future call: split into two repos, or
  fully nest SafeDoc under `projects/safedoc/`.
- The two committed secrets are the only urgent items.
- The 6.1 GB `openclaw/` and 137 MB `venv/` are bloat but not dangerous.

---

## 2. trader-workstation-platform — file-by-file audit

| File | What it does | Matches on-prem plan? | Latent bugs | Verdict |
|---|---|---|---|---|
| `CLAUDE.md` | In-repo project brief for Claude Code. | **No** before this audit — said "PostgreSQL by default" and "deploy to Azure." **Yes** after the rewrite. | — | **Edited.** |
| `README.md` | Public-facing project overview. | **No** before — same Postgres/Azure issues, plus an architecture diagram showing a React/Vue frontend and cloud-ish stack. **Yes** after. | — | **Edited.** |
| `.gitignore` | Standard Python/Docker/IDE ignore list. | Yes. | — | Keep. |
| `docker-compose.yml` | One-service compose for `backend`. Build context already correctly set to `./backend`. Obsolete `version: '3.9'` was removed (by you or a linter) between sessions. | Yes — Phase 2 will add `oracle` and Phase 3 will add `nginx` services here. | — | Keep. |
| `backend/app/main.py` | FastAPI app with `GET /` and `GET /health`, both with pydantic models. | Yes. | — | Keep. |
| `backend/requirements.txt` | Pinned `fastapi`, `uvicorn`, `pydantic`, `pydantic-settings`. | Yes. `pydantic-settings` will earn its keep in Phase 2.7. | `pydantic-settings` is installed but unused. | Keep. |
| `backend/Dockerfile` | Builds the backend image. | Partially — base image will be swapped to AlmaLinux in Phase 1.3. | **Was broken**: `HEALTHCHECK` imported `requests` which isn't installed. Probe failed silently every run. | **Edited** — swapped `requests` → `urllib.request`. |
| `frontend/.gitkeep` | Placeholder for a trader UI. | Partial — the Notion plan only calls for a static placeholder page served by nginx (Phase 3.3), not a SPA. | — | Keep for now; may consolidate into `nginx/` later. |
| `database/.gitkeep` | Placeholder for Oracle init scripts. | Yes — Phase 2.3 writes `database/init.sql`. | — | Keep. |
| `nginx/.gitkeep` | Placeholder for nginx config. | Yes — Phase 3 writes `nginx/nginx.conf`. | — | Keep. |
| `ansible/.gitkeep` | Placeholder for Ansible playbooks. | Yes — Phases 5–6 fill this in. | — | Keep. |
| `ci/.gitkeep` | Placeholder for pipeline definitions. | Yes — Phase 8 writes `ci/azure-pipelines.yml`. | — | Keep. |
| `docs/.gitkeep` | Placeholder for architecture notes. | Yes — Phase 7.3 writes `docs/architecture.md`. | — | Keep. |
| `notes/journal.md` | Study log. | Yes. | Entry was stale ("scaffolding only — nothing runs yet"). | **Edited** — rewrote 2026-05-18 entry to match reality. |
| `notes/audit-2026-05-18.md` | This file. | Yes. | — | **New.** |
| `app/` (stray, empty) | Accidentally created during the docker-compose debugging detour. | No. | — | **Deleted.** |

---

## 3. Changes made in this pass

All changes are inside `trader-workstation-platform/`. Nothing touched in
the parent DevOps-Lab repo per the constraint.

1. **Deleted** stray empty `app/` at the project root.
2. **Edited** `backend/Dockerfile` — replaced the broken `requests`-based
   `HEALTHCHECK` with a stdlib `urllib.request` one. Also rewrote the inline
   comments to explain *why* each block exists, flagged the temporary base
   image, and noted the Phase-4 follow-up.
3. **Edited** `CLAUDE.md` — rewrote the Goal, Stack, Project, Core rules,
   pre-confirmation list, Termius note, and Project progression sections to
   match the on-prem plan from the Notion note. Added an explicit
   "Out of scope" section. Pointed at the Notion note as the authoritative
   roadmap.
4. **Edited** `README.md` — replaced wholesale. New version reflects on-prem
   topology, Oracle (not Postgres), AlmaLinux base, VMware Fusion VMs, RDP-
   simulating-Citrix, Azure DevOps Pipelines with self-hosted agent, and
   the 8-phase roadmap from the Notion note. Architecture diagram redrawn
   to show the trader-workstation flow.
5. **Edited** `notes/journal.md` — rewrote the 2026-05-18 entry to honestly
   reflect what's on disk and log the cleanups done today.
6. **Added** `notes/audit-2026-05-18.md` (this file).

---

## 4. Verified after the changes

- `docker-compose.yml` build context still points at `./backend` and the
  COPY paths in the Dockerfile resolve correctly.
- No edits to `backend/app/main.py`, `requirements.txt`, or `.gitignore` —
  those are correct as-is.
- The HEALTHCHECK fix is stdlib-only; no new packages needed.
- The Dockerfile rewrite preserved the original layer order so Docker's
  build cache stays valid; the `urllib.request` change is in the
  HEALTHCHECK metadata, not in a build step, so it doesn't invalidate
  cached layers.

I **did not** rebuild the image. You should re-run `docker-compose up --build`
to confirm the image still builds and the new HEALTHCHECK reports
`healthy` (visible via `docker ps`).

---

## 5. Parent-repo cleanup — what was done after approval

Same-day update: after your "I agree to everything, proceed," I executed the
parent-repo cleanup. Summary below; details in the Git summary in section 6.

### Done — secrets fully scrubbed from the remote

1. **`scripts/gcp-credentials.json`** — GCP key (ID `5f69b31c`) revoked by
   you upstream. File removed from current tree and from **all 13 commits
   on `main`** via `git filter-branch`. Force-pushed
   (`--force-with-lease`) to `origin/main`. The pre-rewrite top of
   `origin/main` was `fafa039`; the rewritten top is `e7b6007`. Verified
   with `git log origin/main --oneline -- scripts/gcp-credentials.json`
   (empty result).
2. **`projects/safedoc/config/gcp-credentials.json`** — second tracked
   copy of the same GCP key. Same treatment, same verification.
3. **`github-recovery-codes.txt`** — GitHub 2FA codes regenerated by you
   upstream. File removed from current tree and all history. Verified.

### Known remaining: local-only blob remnant

The `copilot/worktree-2026-04-28T11-16-48` branch (local only, never
pushed) still references commit `32d7128`, which originally introduced the
secrets. That keeps the secret blobs in the local git object database
even after `git gc`. **No network exposure** — the secrets exist only in
your local `.git/objects/`, not on GitHub. To fully evict them locally,
delete the worktree and the branch, then `git gc --prune=now`. Not urgent.

### Done — structural cleanups

4. **`.gitmodules`** removed. The orphan `openclaw` submodule entry is
   gone; `openclaw/` directory was already absent from disk before this
   session. The submodule reference in HEAD's tree is staged for removal
   in the next commit.
5. **SafeDoc duplicates at the repo root** removed via `git rm`: identical
   copies live under `projects/safedoc/`. Files removed: `commands.txt`,
   `diagram_business_Piotr_Reszka.bpmn`, `diagram_business_Piotr_Reszka.png`,
   `dummy_invoice.pdf`, `input_invoices/` (3 files), `insert_test_data.py`,
   `process_all.sh`, `scripts/backend_app.py`, `scripts/setup_db.py`,
   `scripts/view_invoices.sql`.
   - `process_all.sh` at the root differed slightly from the SafeDoc-local
     copy — only `INVOICE_DIR` and the script path; the SafeDoc-local copy
     is the current/correct version.
6. **`venv/`** confirmed not tracked (`git ls-files venv/` returned 0
   entries). The 137 MB on disk is your call — left untouched.
7. **`marp_presentations/`** was deleted from disk (only contained an
   empty `images/` subdirectory).

### Done — `.gitignore` hardened

Added patterns to prevent future credential commits: `.env`, `.env.local`,
`*credentials*.json`, `*service-account*.json`, `*-key.json`,
`*recovery-codes*`, `*.key`, `*.pem`.

### Still your call — trader-workstation-platform design

8. **`frontend/` directory** — keep for now, fold or delete in Phase 3.
9. **`pydantic-settings`** unused now, wired in Phase 2.7.

### Optional follow-ups

10. Delete `copilot/worktree-2026-04-28T11-16-48` worktree + branch when
    no longer needed, then `git gc --prune=now`.
11. `venv/` on disk — delete if not in use.
12. `web_app/app.py` modified — your in-flight work, untouched.

---

## 6. Git summary (for GitKraken)

### What has already happened on the remote

`origin/main` was force-pushed during this session. Old top was `fafa039`,
new top is `e7b6007`. All 13 commits had the three secret files purged from
their trees; the 4 commits that originally added or modified those files
got new SHAs (commits before that point kept their original SHAs because
they were content-equivalent after the filter). If you had any other clones
of `moj-lab-devops` lying around, they need to be re-cloned or hard-reset.

### What's pending in your working tree, to commit from GitKraken

Two repos worth of changes are sitting uncommitted. The whole
`trader-workstation-platform/` directory is **still untracked** — your
previous commits were SafeDoc-only. So the first commit will literally add
the project to the repo.

```
working tree (DevOps-Lab repo, all uncommitted):
 M .gitignore                                       (hardened with secret patterns)
 D commands.txt                                     (duplicate, lives in projects/safedoc/docs/)
 D diagram_business_Piotr_Reszka.bpmn               (duplicate)
 D diagram_business_Piotr_Reszka.png                (duplicate)
 D dummy_invoice.pdf                                (duplicate)
 D input_invoices/*  (3 files)                      (duplicates)
 D insert_test_data.py                              (duplicate)
 D openclaw                                         (orphan submodule reference)
 D process_all.sh                                   (slightly older copy of SafeDoc's)
 D scripts/backend_app.py                           (duplicate)
 D scripts/setup_db.py                              (duplicate)
 D scripts/view_invoices.sql                        (duplicate)
 M web_app/app.py                                   (your in-flight work — leave alone)
?? Trading_App_Note.pdf                             (the Notion roadmap export — your call)
?? trader-workstation-platform/                     (the whole new project)
```

### Suggested commit grouping (three commits)

**Commit 1 — bootstrap the trader-workstation-platform**
Stage in GitKraken: the entire `trader-workstation-platform/` directory.
Suggested message:

```
feat: bootstrap trader-workstation-platform on-prem DevOps lab

Initial Phase 0 + Phase 1.1/1.2 state: runnable FastAPI backend in Docker,
on-prem-aligned brief and README, audit and journal entries, scaffolding
for nginx/ansible/database/ci/docs.
```

**Commit 2 — parent-repo housekeeping**
Stage in GitKraken: `.gitignore` (M), all the `D` deletions of duplicate
SafeDoc files, the `D openclaw` submodule deletion. Do **not** stage
`web_app/app.py` (your in-flight work).
Suggested message:

```
chore: remove SafeDoc duplicates at repo root, drop orphan openclaw
submodule, harden .gitignore against secrets
```

**Commit 3 (optional) — `Trading_App_Note.pdf`**
If you want the Notion export tracked, stage and commit it. If not, leave
it untracked (or `.gitignore` it).

### What to look at in GitKraken

- **Graph**: after fetch, you should see your local `main` and `origin/main`
  at the same `e7b6007` commit. Before your new commits, the four most
  recent commits in the graph (`ab6070f → 6a6bb14 → 456e865 → e7b6007`)
  will have **different SHAs than yesterday's view**. If GitKraken shows
  cached references to the old SHAs (`32d7128`, `4f1d475`, `5ad9d64`,
  `fafa039`), refresh the repo.
- **Working tree pane**: 12 deletions + 1 modification + 1 large untracked
  tree.
- After Commit 1, the file pane should empty out the `trader-workstation-platform/`
  untracked block.
- After Commit 2, the file pane should be clean except for `web_app/app.py`
  and `Trading_App_Note.pdf`.
- Do **not** force-push again — `main` and `origin/main` are already in
  sync after the rewrite. Regular `git push` will move them forward
  together.
